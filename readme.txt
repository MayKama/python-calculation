std_d
Given a database, develop a Python program with the following features
1. The every student has a reg number that is unique and is used to identify
each student
2. Each of the students have three courses that are recorded
    a. Maths
    b. English
    c. Computer Science
3. For each of these courses the student has a score, and it should be secured,
and will not be changed.

to run the code, firstly import the file: 
1. import std_dict as sd
2. Add data inside the database: sd.addStudent()
3. to check whata i in the database, s.printStudents()

to reload the the database, this will delete the exissting info
4. from importlib import reload
to directly access the list in the database, use the reference function sd
5. sd.database


